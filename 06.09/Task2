// Example program
#include <iostream>
#include <string>
#include <math.h>

int main()
{
 int a = 2;
 double b = 13.17;
 double result = 0;
 
 result = pow((pow(M_E, 0.5 * log((1-cos((a/b) * M_PI))/(1 + cos((a/b) * M_PI)))))+(pow(M_E, 0.3 * log((1-sin((a/b) * M_PI))/(1 + sin((a/b) * M_PI))))), 0.5);
 
 std::cout << result;
}
